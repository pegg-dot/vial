import type { Metadata } from "next";import { Activity, CheckCircle2, FlaskConical, Target } from "lucide-react";import { requirePermission } from "@/server/auth/session";import { getBenchmarkDashboard } from "@/server/market-data/benchmarks";import { runBenchmarkAction } from "../market-data-actions";
export const metadata:Metadata={title:"Parser benchmarks"};export const dynamic="force-dynamic";
export default async function Page(){await requirePermission("catalog:read");const data=await getBenchmarkDashboard();const latest=data.runs[0];return <div><p className="text-[11px] font-semibold uppercase tracking-[.18em] text-[var(--muted)]">Golden-set evaluation</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.055em] sm:text-5xl">Parser benchmarks</h1><p className="mt-4 max-w-3xl text-sm leading-6 text-[var(--muted)]">Versioned parser contracts run against human-labeled fictional pages. Promotion depends on measured precision, recall, edge-case coverage, and correction rates.</p><div className="mt-8 grid gap-4 sm:grid-cols-4"><Metric icon={Target} value={latest?`${Math.round(latest.f1*100)}%`:'—'} label="Latest F1"/><Metric icon={CheckCircle2} value={`${data.runs.filter(r=>r.status==='passed').length}/${data.runs.length}`} label="Passing runs"/><Metric icon={FlaskConical} value={String(data.datasets.reduce((s,d)=>s+d.examples,0))} label="Golden examples"/><Metric icon={Activity} value={String(data.contracts.filter(c=>c.status==='active').length)} label="Active contracts"/></div><form action={runBenchmarkAction} className="mt-6"><button className="rounded-full bg-black px-5 py-3 text-xs font-semibold text-white">Run catalog benchmark</button></form><section className="mt-7 overflow-hidden rounded-[28px] border border-black/[.07] bg-white"><div className="border-b border-black/[.06] p-6"><h2 className="text-xl font-semibold">Evaluation history</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-black/[.025] text-[10px] uppercase tracking-[.12em] text-[var(--muted)]"><tr><th className="px-6 py-4">Profile</th><th>Status</th><th>Precision</th><th>Recall</th><th>F1</th><th>Exact examples</th><th>Errors</th><th>Run</th></tr></thead><tbody className="divide-y divide-black/[.06]">{data.runs.map(run=><tr key={run.id}><td className="px-6 py-4 font-mono text-xs">{run.profile_key}</td><td><span className={`rounded-full px-2.5 py-1 text-[10px] font-semibold ${run.status==='passed'?'bg-emerald-50 text-emerald-700':'bg-rose-50 text-rose-700'}`}>{run.status}</span></td><td>{Math.round(run.precision*100)}%</td><td>{Math.round(run.recall*100)}%</td><td className="font-semibold">{Math.round(run.f1*100)}%</td><td>{Math.round(run.field_accuracy*100)}%</td><td>{run.false_positives} FP · {run.false_negatives} FN</td><td className="text-xs text-[var(--muted)]">{new Date(run.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></section><section className="mt-6 grid gap-4 lg:grid-cols-2">{data.contracts.map(contract=><article key={contract.id} className="rounded-[24px] border border-black/[.07] bg-white p-6"><div className="flex items-start justify-between"><div><p className="font-mono text-xs text-violet-700">{contract.profile_key} v{contract.version}</p><h2 className="mt-2 text-xl font-semibold">{contract.source_type}</h2></div><span className="rounded-full bg-black/[.04] px-2.5 py-1 text-[10px] font-semibold">{contract.status}</span></div><p className="mt-4 text-xs text-[var(--muted)]">Extractor {contract.extractor_version}</p><div className="mt-4 flex flex-wrap gap-2">{contract.required_fields.map((field:string)=><span key={field} className="rounded-full border border-black/[.07] px-2.5 py-1 text-[10px]">{field}</span>)}</div></article>)}</section></div>}
function Metric({icon:Icon,value,label}:{icon:React.ComponentType<{className?:string}>;value:string;label:string}){return <div className="rounded-[22px] border border-black/[.07] bg-white p-5"><Icon className="size-4 text-black/30"/><p className="mt-5 text-3xl font-semibold">{value}</p><p className="mt-1 text-xs text-[var(--muted)]">{label}</p></div>}
