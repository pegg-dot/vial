import type { Metadata } from "next";import { GitBranch, Network, Tags, Unplug } from "lucide-react";import { requirePermission } from "@/server/auth/session";import { getEntityDashboard } from "@/server/market-data/graph";import { rebuildGraphAction } from "../market-data-actions";
export const metadata:Metadata={title:"Entity graph"};export const dynamic="force-dynamic";
export default async function Page(){await requirePermission("catalog:read");const data=await getEntityDashboard();const total=data.summary.types.reduce((sum,row)=>sum+row.count,0);return <div><Header eyebrow="Canonical market graph" title="Entities and relationships" description="One identity layer across vendors, compounds, products, listings, sources, laboratories, and declared batches."/><div className="mt-7 grid gap-4 sm:grid-cols-4"><Metric icon={Network} value={total} label="Canonical entities"/><Metric icon={GitBranch} value={data.relationships.length} label="Visible relationships"/><Metric icon={Tags} value={data.entities.reduce((s,e)=>s+e.alias_count,0)} label="Aliases"/><Metric icon={Unplug} value={data.summary.pending} label="Pending resolution"/></div><form action={rebuildGraphAction} className="mt-6"><button className="rounded-full bg-black px-5 py-3 text-xs font-semibold text-white">Rebuild canonical graph</button></form><section className="mt-7 overflow-hidden rounded-[28px] border border-black/[.07] bg-white"><div className="border-b border-black/[.06] p-6"><h2 className="text-xl font-semibold">Canonical entities</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-black/[.025] text-[10px] uppercase tracking-[.12em] text-[var(--muted)]"><tr><th className="px-6 py-4">Entity</th><th>Type</th><th>Aliases</th><th>Relationships</th><th>Status</th></tr></thead><tbody className="divide-y divide-black/[.06]">{data.entities.map(e=><tr key={e.id}><td className="px-6 py-4 font-semibold">{e.display_name}<span className="mt-1 block font-mono text-[10px] font-normal text-black/35">{e.canonical_key}</span></td><td>{e.entity_type}</td><td>{e.alias_count}</td><td>{e.relationship_count}</td><td>{e.status}</td></tr>)}</tbody></table></div></section><section className="mt-6 rounded-[28px] border border-black/[.07] bg-white p-6"><h2 className="text-xl font-semibold">Recent graph edges</h2><div className="mt-5 grid gap-2">{data.relationships.slice(0,20).map((r,index)=><div key={`${r.from_name}:${r.relation_type}:${r.to_name}:${index}`} className="grid gap-2 rounded-2xl bg-[var(--background)] p-4 text-sm sm:grid-cols-[1fr_auto_1fr_auto]"><span className="font-semibold">{r.from_name}</span><span className="text-[var(--muted)]">{r.relation_type.replace(/_/g," ")}</span><span className="font-semibold">{r.to_name}</span><span className="text-xs text-[var(--muted)]">{Math.round(r.confidence*100)}%</span></div>)}</div></section></div>}
function Header({eyebrow,title,description}:{eyebrow:string;title:string;description:string}){return <><p className="text-[11px] font-semibold uppercase tracking-[.18em] text-[var(--muted)]">{eyebrow}</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.055em] sm:text-5xl">{title}</h1><p className="mt-4 max-w-3xl text-sm leading-6 text-[var(--muted)]">{description}</p></>};function Metric({icon:Icon,value,label}:{icon:React.ComponentType<{className?:string}>;value:number;label:string}){return <div className="rounded-[22px] border border-black/[.07] bg-white p-5"><Icon className="size-4 text-black/30"/><p className="mt-5 text-3xl font-semibold">{value}</p><p className="mt-1 text-xs text-[var(--muted)]">{label}</p></div>}
