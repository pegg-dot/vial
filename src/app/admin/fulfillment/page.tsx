import { requirePermission } from "@/server/auth/session";
import { operationsDashboard } from "@/server/commerce/operations";
import { CommerceTable } from "@/components/commerce-table";
export default async function Page(){await requirePermission("commerce:write");const data=await operationsDashboard();const rows=data.shipments as Array<Record<string,unknown>>;return <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-violet-600">Commerce operations</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Fulfillment</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--muted)]">Track seller-specific shipment records and customer-facing delivery states.</p><div className="mt-7"></div><div className="mt-7"><CommerceTable rows={rows} columns={[{key:"id",label:"Shipment"},{key:"order_id",label:"Order"},{key:"seller_id",label:"Seller"},{key:"carrier",label:"Carrier"},{key:"tracking_code",label:"Tracking"},{key:"status",label:"Status"}]} /></div></div>}
