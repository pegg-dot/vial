import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("laboratories:manage");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#2b31d8]">Evidence network</p><h1 className="mt-2 text-4xl font-extrabold tracking-[-.05em]">Laboratory oversight</h1><p className="mt-3 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">Accreditation is never generalized beyond its stated scope. Production onboarding would require direct accreditation-body and method-scope verification.</p><div className="mt-8 space-y-4">{d.laboratories.map((lab:Record<string,unknown>)=><section key={String(lab.id)} className="ink hard rounded-[20px] bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-extrabold">{String(lab.display_name)}</h2><p className="mt-1 text-xs font-medium text-[var(--muted)]">{String(lab.location||"No location")} · {String(lab.accreditation_body||"No accreditation body")}</p></div><span className="ink-1 rounded-full bg-[#e6fbf4] px-3 py-1 text-xs font-extrabold capitalize text-[#0e8f80]">{String(lab.accreditation_status).replaceAll("_"," ")}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[["Status",lab.status],["Onboarding",lab.onboarding_status],["Accreditation #",lab.accreditation_number||"None"],["Expires",lab.accreditation_expires_at?new Date(String(lab.accreditation_expires_at)).toLocaleDateString():"Not set"]].map(([l,v])=><div key={String(l)} className="ink-1 rounded-[12px] bg-[#f7f7f4] p-4"><p className="text-[10px] font-bold uppercase text-[var(--muted)]">{String(l)}</p><p className="mt-2 text-sm font-bold capitalize">{String(v).replaceAll("_"," ")}</p></div>)}</div></section>)}</div></div>}
