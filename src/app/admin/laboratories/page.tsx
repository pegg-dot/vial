import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("laboratories:manage");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-700">Evidence network</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Laboratory oversight</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-black/55">Accreditation is never generalized beyond its stated scope. Production onboarding would require direct accreditation-body and method-scope verification.</p><div className="mt-8 space-y-4">{d.laboratories.map((lab:Record<string,unknown>)=><section key={String(lab.id)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{String(lab.display_name)}</h2><p className="mt-1 text-xs text-black/45">{String(lab.location||"No location")} · {String(lab.accreditation_body||"No accreditation body")}</p></div><span className="rounded-full bg-cyan-50 px-3 py-1 text-xs font-semibold capitalize text-cyan-800">{String(lab.accreditation_status).replaceAll("_"," ")}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[["Status",lab.status],["Onboarding",lab.onboarding_status],["Accreditation #",lab.accreditation_number||"None"],["Expires",lab.accreditation_expires_at?new Date(String(lab.accreditation_expires_at)).toLocaleDateString():"Not set"]].map(([l,v])=><div key={String(l)} className="rounded-2xl bg-black/[.03] p-4"><p className="text-[10px] uppercase text-black/35">{String(l)}</p><p className="mt-2 text-sm font-semibold capitalize">{String(v).replaceAll("_"," ")}</p></div>)}</div></section>)}</div></div>}
