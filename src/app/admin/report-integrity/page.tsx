import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("evidence:read");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-rose-700">Quality oversight</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Report integrity</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-black/55">Issued, amended, superseded, and revoked reports remain in the historical record. Open conflicts are explicit and block simplified conclusions.</p><section className="mt-8"><h2 className="text-xl font-semibold">Reports</h2><div className="mt-4 space-y-3">{d.reports.map((r:Record<string,unknown>)=><div key={String(r.id)} className="rounded-[24px] border border-black/[.07] bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-mono text-xs text-violet-700">{String(r.report_number)} · v{String(r.version)}</p><p className="mt-2 font-semibold">{String(r.laboratory_name)} · Sample {String(r.sample_code)}</p></div><span className="rounded-full bg-black/[.05] px-3 py-1 text-xs font-semibold capitalize">{String(r.status)}</span></div></div>)}</div></section><section className="mt-8"><h2 className="text-xl font-semibold">Open conflicts</h2><div className="mt-4 space-y-3">{d.conflicts.map((c:Record<string,unknown>)=><div key={String(c.id)} className="rounded-[24px] border border-amber-200 bg-amber-50 p-5"><div className="flex items-center justify-between"><p className="font-semibold capitalize">{String(c.dimension)} · {String(c.declared_batch_code)}</p><span className="text-xs font-semibold uppercase text-amber-800">{String(c.severity)}</span></div><p className="mt-2 text-sm leading-6 text-amber-950/65">{String(c.explanation)}</p></div>)}</div></section></div>}
