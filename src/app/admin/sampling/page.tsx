import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("evidence:write");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-700">Independent sampling</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">Programs and collection models</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-black/55">Sampling provenance controls the strength of the inference. Blind and repeated sampling are tracked separately from vendor-submitted units.</p><div className="mt-8 space-y-4">{d.programs.map((p:Record<string,unknown>)=><section key={String(p.id)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-wide text-cyan-700">{String(p.sampling_model).replaceAll("_"," ")}</p><h2 className="mt-2 text-xl font-semibold">{String(p.name)}</h2><p className="mt-2 max-w-3xl text-sm text-black/45">{String(p.public_summary)}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-800">{String(p.status)}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[["Samples",`${p.collected_sample_count}/${p.target_sample_count}`],["Funding",`$${Number(p.funding_collected).toFixed(0)} / $${Number(p.funding_target).toFixed(0)}`],["Sponsor",p.sponsor_type],["Subject",p.subject_type]].map(([l,v])=><div key={String(l)} className="rounded-2xl bg-black/[.03] p-4"><p className="text-[10px] uppercase text-black/35">{String(l)}</p><p className="mt-2 text-sm font-semibold capitalize">{String(v)}</p></div>)}</div></section>)}</div></div>}
