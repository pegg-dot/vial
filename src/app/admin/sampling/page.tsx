import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("evidence:write");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#2b31d8]">Independent sampling</p><h1 className="mt-2 text-4xl font-extrabold tracking-[-.05em]">Programs and collection models</h1><p className="mt-3 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">Sampling provenance controls the strength of the inference. Blind and repeated sampling are tracked separately from vendor-submitted units.</p><div className="mt-8 space-y-4">{d.programs.map((p:Record<string,unknown>)=><section key={String(p.id)} className="ink hard rounded-[20px] bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-extrabold uppercase tracking-wide text-[#2b31d8]">{String(p.sampling_model).replaceAll("_"," ")}</p><h2 className="mt-2 text-xl font-extrabold">{String(p.name)}</h2><p className="mt-2 max-w-3xl text-sm font-medium text-[var(--muted)]">{String(p.public_summary)}</p></div><span className="ink-1 rounded-full bg-[#e6fbf4] px-3 py-1 text-xs font-extrabold text-[#0e8f80]">{String(p.status)}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[["Samples",`${p.collected_sample_count}/${p.target_sample_count}`],["Funding",`$${Number(p.funding_collected).toFixed(0)} / $${Number(p.funding_target).toFixed(0)}`],["Sponsor",p.sponsor_type],["Subject",p.subject_type]].map(([l,v])=><div key={String(l)} className="ink-1 rounded-[12px] bg-[#f7f7f4] p-4"><p className="text-[10px] font-bold uppercase text-[var(--muted)]">{String(l)}</p><p className="mt-2 text-sm font-bold capitalize">{String(v)}</p></div>)}</div></section>)}</div></div>}
