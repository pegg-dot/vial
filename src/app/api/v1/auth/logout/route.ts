import{NextResponse}from"next/server";import{cookies}from"next/headers";import{decodeSessionEnvelope,SESSION_COOKIE}from"@/server/auth/session-envelope";import{revokeSession}from"@/server/auth/repository";export async function POST(){const c=await cookies(),e=decodeSessionEnvelope(c.get(SESSION_COOKIE)?.value);if(e)await revokeSession(e.sessionId);const r=NextResponse.json({authenticated:false});r.cookies.set(SESSION_COOKIE,"",{httpOnly:true,sameSite:"strict",secure:process.env.NODE_ENV==="production",path:"/",maxAge:0});return r}
