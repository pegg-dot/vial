import{NextRequest,NextResponse}from"next/server";import{createSandboxCheckout}from"@/server/commerce/repository";import{requireApiPrincipal}from"@/server/auth/principal";export async function POST(r:NextRequest){const a=await requireApiPrincipal({accountTypes:["customer","seller"]});if(a.response)return a.response;try{const b=await r.json();return NextResponse.json(await createSandboxCheckout({customerKey:a.principal.id,email:a.principal.email,address:{line1:String(b.address?.line1||""),city:String(b.address?.city||""),region:String(b.address?.region||""),postalCode:String(b.address?.postalCode||"")},idempotencyKey:String(b.idempotencyKey||crypto.randomUUID())}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Checkout failed"},{status:400})}}
