import{NextRequest,NextResponse}from"next/server";import{createSandboxDispute}from"@/server/commerce/operations";import{requireApiPermission}from"@/server/auth/principal";export async function POST(r:NextRequest){const a=await requireApiPermission("finance:write");if(a.response)return a.response;try{const b=await r.json();return NextResponse.json(await createSandboxDispute({orderId:String(b.orderId),amount:Number(b.amount),reason:String(b.reason||"Sandbox dispute")}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Dispute failed"},{status:400})}}
