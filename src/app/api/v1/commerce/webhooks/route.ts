import{NextRequest,NextResponse}from"next/server";import{recordWebhook,replayWebhook}from"@/server/commerce/operations";import{requireApiPermission}from"@/server/auth/principal";export async function POST(r:NextRequest){const a=await requireApiPermission("admin:manage");if(a.response)return a.response;try{const b=await r.json();return NextResponse.json(b.action==="replay"?await replayWebhook(String(b.id)):await recordWebhook({providerEventId:String(b.providerEventId||crypto.randomUUID()),eventType:String(b.eventType||"sandbox.event"),payload:b.payload||{}}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Webhook failed"},{status:400})}}
