import type { Metadata } from "next";
import Link from "next/link";
import { ArrowUpRight, Building2, CircleAlert, ExternalLink, FlaskConical, Landmark, ScrollText, ShieldQuestion, Store } from "lucide-react";
import { ArtCoa, ArtShieldCheck, VialBuddy } from "@/components/vial-art";
import { PURITY_IS_NOT_GRADE } from "@/lib/provenance-copy";

export const metadata: Metadata = {
  title: "Purity vs grade",
  description: "PLACEHOLDER",
};

export default function GradesPage() {
  return <div />;
}
