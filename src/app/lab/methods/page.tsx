import { requireLaboratoryPermission } from "@/server/auth/session";
import { getLaboratoryContext } from "@/server/evidence-network/repository";
import { LabHeader, StatusPill } from "@/components/laboratory/laboratory-ui";
function arr(v:unknown){if(Array.isArray(v))return v;try{return JSON.parse(String(v))}catch{return[]}}
export default async function Page(){const p=await requireLaboratoryPermission("lab:methods:read");const c=await getLaboratoryContext(p.email);if(!c)return null;return <div><LabHeader eyebrow="Method registry" title="A laboratory is competent for methods, not everything." description="Each method has a version, technique, matrix, analyte, dimensions, limits, uncertainty, validation state, and accreditation-scope indicator."/><div className="mt-8 grid gap-4 xl:grid-cols-2">{c.methods.map((m:Record<string,unknown>)=><section key={String(m.id)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-xs text-cyan-700">{String(m.method_code)} · v{String(m.version)}</p><h2 className="mt-2 text-xl font-semibold">{String(m.name)}</h2><p className="mt-1 text-sm text-black/45">{String(m.technique)}</p></div><StatusPill value={m.validation_status}/></div><div className="mt-5 flex flex-wrap gap-2">{arr(m.dimensions).map((x:unknown)=><span key={String(x)} className="rounded-full bg-cyan-50 px-3 py-1 text-xs font-semibold text-cyan-800">{String(x)}</span>)}{Boolean(m.accreditation_covered)&&<span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-800">Scope covered</span>}</div><dl className="mt-5 grid grid-cols-3 gap-3 text-sm"><div><dt className="text-black/35">LOD</dt><dd className="mt-1 font-semibold">{m.limit_of_detection==null?"N/A":String(m.limit_of_detection)}</dd></div><div><dt className="text-black/35">LOQ</dt><dd className="mt-1 font-semibold">{m.limit_of_quantitation==null?"N/A":String(m.limit_of_quantitation)}</dd></div><div><dt className="text-black/35">Uncertainty</dt><dd className="mt-1 font-semibold">{m.uncertainty==null?"N/A":String(m.uncertainty)}</dd></div></dl></section>)}</div></div>}
