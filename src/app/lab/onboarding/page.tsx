import { requireLaboratoryPermission } from "@/server/auth/session";
import { getLaboratoryContext } from "@/server/evidence-network/repository";
import { LabHeader, StatusPill } from "@/components/laboratory/laboratory-ui";
export default async function Page(){const p=await requireLaboratoryPermission("lab:onboarding:write");const c=await getLaboratoryContext(p.email);if(!c)return null;return <div><LabHeader eyebrow="Laboratory onboarding" title="Competence is scoped, not assumed." description="Identity, impartiality, quality-system, method scope, team roles, security, and agreements are reviewed independently before a laboratory can participate."/><div className="mt-8 rounded-[28px] border border-black/[.07] bg-white p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-black/45">Completion</p><p className="mt-1 text-3xl font-semibold">{Number(c.onboarding?.completion_percent??0)}%</p></div><StatusPill value={c.onboarding?.status}/></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-black/[.06]"><div className="h-full rounded-full bg-gradient-to-r from-cyan-600 to-violet-500" style={{width:`${Number(c.onboarding?.completion_percent??0)}%`}}/></div></div><div className="mt-6 grid gap-3 md:grid-cols-2">{c.steps.map((s:Record<string,unknown>)=><div key={String(s.id)} className="rounded-[24px] border border-black/[.07] bg-white p-5"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold capitalize">{String(s.step_key).replaceAll("_"," ")}</h2><StatusPill value={s.status}/></div><p className="mt-3 text-xs leading-5 text-black/45">This dimension is independently reviewable and cannot be completed by a report upload alone.</p></div>)}</div></div>}
