import { requireLaboratoryPermission } from "@/server/auth/session";
import { getLaboratoryContext } from "@/server/evidence-network/repository";
import { LabHeader, StatusPill } from "@/components/laboratory/laboratory-ui";
export default async function Page(){const p=await requireLaboratoryPermission("lab:orders:read");const c=await getLaboratoryContext(p.email);if(!c)return null;return <div><LabHeader eyebrow="Test orders" title="One request, explicit scope." description="Every order records sponsor, subject, sampling model, requested dimensions, assigned methods, due date, and a root provenance event."/><div className="mt-8 space-y-4">{c.orders.map((o:Record<string,unknown>)=><section key={String(o.id)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{String(o.order_number)}</h2><p className="mt-1 text-xs text-black/45">{String(o.program_name||"Direct order")} · Batch {String(o.declared_batch_code||"unassigned")}</p></div><StatusPill value={o.status}/></div><div className="mt-5 grid gap-3 sm:grid-cols-4">{[["Requester",o.requester_type],["Priority",o.priority],["Quoted",`$${Number(o.quoted_amount??0).toFixed(2)}`],["Due",o.due_at?new Date(String(o.due_at)).toLocaleDateString():"Not set"]].map(([l,v])=><div key={String(l)} className="rounded-2xl bg-black/[.03] p-4"><p className="text-[10px] uppercase tracking-wide text-black/35">{String(l)}</p><p className="mt-2 text-sm font-semibold capitalize">{String(v)}</p></div>)}</div></section>)}</div></div>}
