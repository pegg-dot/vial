import { notFound } from "next/navigation";
import Link from "next/link";
import { getPublicLaboratory } from "@/server/evidence-network/repository";
export const dynamic = "force-dynamic";
function arr(v:unknown){if(Array.isArray(v))return v;try{return JSON.parse(String(v))}catch{return[]}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await getPublicLaboratory(slug);if(!data)notFound();const lab=data.lab as Record<string,unknown>;return <div className="mx-auto max-w-[1320px] px-5 py-16 sm:px-8 sm:py-24"><p className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-700">Laboratory profile</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.065em] sm:text-7xl">{String(lab.display_name)}</h1><p className="mt-5 max-w-3xl text-base leading-7 text-black/55">{String(lab.description)}</p><div className="mt-8 grid gap-3 sm:grid-cols-4">{[["Status",lab.status],["Accreditation",lab.accreditation_status],["Quality system",lab.quality_system],["Reports",data.reports.length]].map(([l,v])=><div key={String(l)} className="rounded-[22px] border border-black/[.07] bg-white p-5"><p className="text-[10px] uppercase tracking-wide text-black/35">{String(l)}</p><p className="mt-2 text-sm font-semibold capitalize">{String(v).replaceAll("_"," ")}</p></div>)}</div><section className="mt-12"><h2 className="text-3xl font-semibold tracking-[-.04em]">Validated method registry</h2><div className="mt-6 grid gap-4 lg:grid-cols-2">{data.methods.map((m:Record<string,unknown>)=><div key={String(m.id)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><p className="font-mono text-xs text-cyan-700">{String(m.method_code)} · v{String(m.version)}</p><h3 className="mt-2 text-xl font-semibold">{String(m.name)}</h3><p className="mt-1 text-sm text-black/45">{String(m.technique)}</p><div className="mt-4 flex flex-wrap gap-2">{arr(m.dimensions).map((x:unknown)=><span key={String(x)} className="rounded-full bg-cyan-50 px-3 py-1 text-xs font-semibold text-cyan-800">{String(x)}</span>)}</div><p className="mt-5 text-xs leading-5 text-black/45">Accreditation-covered: {Boolean(m.accreditation_covered)?"Recorded as covered in sandbox":"Not recorded as covered"}</p></div>)}</div></section><section className="mt-12"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-black/40">Issued records</p><h2 className="mt-2 text-3xl font-semibold tracking-[-.04em]">Structured report history</h2></div><Link href="/passports" className="text-sm font-semibold text-cyan-700">View batch passports</Link></div><div className="mt-6 space-y-3">{data.reports.map((r:Record<string,unknown>)=><div key={String(r.id)} className="rounded-[24px] border border-black/[.07] bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-mono text-xs text-cyan-700">{String(r.report_number)} · v{String(r.version)}</p><p className="mt-2 font-semibold">Sample {String(r.sample_code)} · Batch {String(r.declared_batch_code)}</p><p className="mt-2 text-sm text-black/45">{String(r.public_summary)}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-800">{String(r.status)}</span></div></div>)}</div></section></div>}
