import type{Metadata}from"next";import{LogIn}from"lucide-react";import{loginAction}from"@/server/auth/actions";export const metadata:Metadata={title:"Sign in",description:"Sign in to your VIAL account."};export default async function Page({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const p=await searchParams;return <section className="mx-auto max-w-[560px] px-5 py-20 sm:px-8"><div className="rounded-[32px] border border-black/[.08] bg-white p-8"><LogIn/><h1 className="mt-6 text-4xl font-semibold">Continue to VIAL.</h1>{p.error&&<p className="mt-4 text-rose-700">{p.error}</p>}<form action={loginAction} className="mt-8 space-y-4"><input type="hidden" name="returnTo" value={p.next||"/account"}/><label className="block">Email<input name="email" type="email" required className="field mt-2"/></label><label className="block">Password<input name="password" type="password" required className="field mt-2"/></label><button className="h-12 w-full rounded-full bg-black text-white">Sign in</button></form>{process.env.NODE_ENV!=="production"&&<p className="mt-6 text-xs">Customer: nora@example.test / VialDemoCustomer!2026<br/>Seller: marcus@helixtest.test / VialDemoSeller!2026<br/>Laboratory: elena@aperture.test / VialDemoLaboratory!2026</p>}</div></section>}
