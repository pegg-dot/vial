import type { Metadata } from "next";
import { Beaker, CheckCircle2, CircleDashed, FileSearch, FlaskConical, Link2, ShieldQuestion } from "lucide-react";
import Link from "next/link";
import { getEvidenceLibraryStats, getObservedIssuers } from "@/server/public-repository";

export const metadata: Metadata = { title: "Lab reports", description: "Understand the reports, methods, and evidence limits behind VIAL records." };
export const dynamic = "force-dynamic";
export default async function ResearchPage(){const [stats,issuerRows]=await Promise.all([getEvidenceLibraryStats(),getObservedIssuers()]); return <div><section className="border-b-2 border-[#111214]"><div className="mx-auto max-w-[1320px] px-5 py-16 sm:px-8 sm:py-24"><p className="text-[11px] font-bold uppercase tracking-[.2em] text-[#2b31d8]">Lab reports</p><h1 className="mt-4 max-w-5xl text-5xl font-extrabold tracking-[-.065em] sm:text-7xl">What the documents establish. What they do not.</h1><p className="mt-6 max-w-2xl text-base font-medium leading-7 text-[var(--muted)]">A lab report answers six different questions &mdash; is it real, is it for this batch, is it the right compound, is the dose there, who picked the sample, is it clean. We answer each one separately instead of blending them into one misleading score.</p><div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Stat icon={FileSearch} value={stats.reports} label="Reports located"/><Stat icon={CheckCircle2} value={stats.confirmed} label="Issuer confirmed"/><Stat icon={Link2} value={stats.linked} label="Batch linked"/><Stat icon={FlaskConical} value={stats.issuers} label="Named issuers"/><Stat icon={CircleDashed} value={stats.stale} label="Stale records"/></div></div></section><section className="mx-auto grid max-w-[1320px] gap-8 px-5 py-16 sm:px-8 sm:py-24 lg:grid-cols-[.78fr_1.22fr]"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-[#2b31d8]">Evidence dimensions</p><h2 className="mt-3 text-4xl font-extrabold tracking-[-.055em]">One report can answer only a narrow question.</h2><p className="mt-5 text-sm font-medium leading-6 text-[var(--muted)]">A report may establish that one submitted sample produced one analytical result. It does not automatically prove that every unit in a batch is identical, sterile, properly stored, or suitable for human use.</p><Link href="/how-we-check" className="ink hard-sm press mt-6 inline-flex rounded-full bg-[#111214] px-5 py-3 text-sm font-bold text-white">See how we check</Link></div><div className="grid gap-3 sm:grid-cols-2"><Dimension title="Document authenticity" text="Was the report genuinely issued by the named laboratory?"/><Dimension title="Batch linkage" text="Does the report identifier connect to the inventory currently being displayed?"/><Dimension title="Chemical identity" text="Did the tested sample match the claimed compound?"/><Dimension title="Declared quantity" text="Was the amount independently measured rather than only stated on the label?"/><Dimension title="Sampling provenance" text="Who selected and submitted the physical sample?"/><Dimension title="Contamination controls" text="Were sterility, endotoxin, or particulates separately evaluated?"/></div></section><section className="border-y-2 border-[#111214] bg-white/60"><div className="mx-auto max-w-[1320px] px-5 py-16 sm:px-8 sm:py-20"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-[#2b31d8]">Observed issuers</p><h2 className="mt-3 text-3xl font-extrabold tracking-[-.045em]">Laboratory names on record</h2></div><p className="max-w-lg text-xs font-medium leading-5 text-[var(--muted)]">These are the laboratories named on the certificates we hold. A named issuer is not an endorsement, accreditation claim, or proof of physical-product quality &mdash; see each lab&rsquo;s profile for what we could verify.</p></div><div className="mt-8 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{issuerRows.map(({lab,count})=><Link key={lab} href={`/labs`} className="ink-1 hard press rounded-[18px] bg-white p-5"><div className="flex items-center gap-3"><span className="ink-1 grid size-10 place-items-center rounded-2xl bg-[#eef0ff]"><Beaker className="size-4 text-[#2b31d8]"/></span><div><h3 className="text-sm font-bold">{lab}</h3><p className="mt-1 text-[10px] uppercase tracking-[.12em] text-[var(--muted)]">{count} certificate{count===1?"":"s"} on record</p></div></div></Link>)}</div></div></section><section className="mx-auto max-w-[1320px] px-5 py-16 sm:px-8 sm:py-24"><div className="ink hard-blue rounded-[20px] bg-[#111214] p-7 text-white sm:p-10"><ShieldQuestion className="size-6 text-[#8fa2ff]"/><h2 className="mt-8 max-w-3xl text-4xl font-extrabold tracking-[-.055em]">Unknown is a valid result.</h2><p className="mt-5 max-w-2xl text-sm leading-6 text-white/55">When VIAL cannot establish a dimension, the record stays unknown. The platform does not infer safety from price, popularity, vendor reputation, or an attractive PDF.</p></div></section></div>}
function Stat({icon:Icon,value,label}:{icon:React.ComponentType<{className?:string}>;value:number;label:string}){return <div className="ink-1 hard rounded-[18px] bg-white p-4"><Icon className="size-4 text-[#2b31d8]"/><p className="mt-5 text-3xl font-extrabold tracking-[-.055em]">{value}</p><p className="mt-1 text-[10px] uppercase tracking-[.12em] text-[var(--muted)]">{label}</p></div>}
function Dimension({title,text}:{title:string;text:string}){return <div className="ink-1 hard rounded-[18px] bg-white p-5"><h3 className="text-sm font-bold">{title}</h3><p className="mt-3 text-xs font-medium leading-5 text-[var(--muted)]">{text}</p></div>}
