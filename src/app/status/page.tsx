import type { Metadata } from "next";
import { CheckCircle2, Database, RadioTower, ShieldCheck } from "lucide-react";
import { getRefreshMetrics } from "@/server/refresh/repository";
import { getIntelligenceMetrics } from "@/server/intelligence/repository";
export const metadata:Metadata={title:"System status"};export const dynamic="force-dynamic";
export default async function StatusPage(){const [refresh,intel]=await Promise.all([getRefreshMetrics(),getIntelligenceMetrics()]);return <div className="mx-auto max-w-[1000px] px-5 py-16 sm:px-8 sm:py-24"><div className="rounded-[34px] bg-[#111214] p-7 text-white sm:p-10"><div className="flex items-center gap-3"><CheckCircle2 className="size-5 text-emerald-300"/><p className="text-sm font-semibold">All prototype systems operational</p></div><h1 className="mt-8 text-5xl font-semibold tracking-[-.065em] sm:text-7xl">System status</h1><p className="mt-5 max-w-2xl text-sm leading-6 text-white/55">Live database and workflow health for the current VIAL environment.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><Card icon={Database} title="Catalog database" value="Operational" detail="Public projections available"/><Card icon={RadioTower} title="Refresh engine" value={`${refresh.enabled} enabled`} detail={`${refresh.queued} queued · ${refresh.stale} stale`}/><Card icon={ShieldCheck} title="Intelligence graph" value={`${intel.traces} traces`} detail={`${intel.alerts} alerts · ${intel.open} open signals`}/></div><div className="mt-8 rounded-[28px] border border-black/[.07] bg-white p-6"><h2 className="text-xl font-semibold tracking-[-.03em]">Prototype scope</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">This page reports application state, not external vendor uptime. The seed transport uses demo fixtures; approved Live sources are fetched over real HTTP.</p></div></div>}
function Card({icon:Icon,title,value,detail}:{icon:React.ComponentType<{className?:string}>;title:string;value:string;detail:string}){return <div className="rounded-[24px] border border-black/[.07] bg-white p-5"><Icon className="size-4 text-emerald-700"/><p className="mt-6 text-sm font-semibold">{title}</p><p className="mt-2 text-2xl font-semibold tracking-[-.045em]">{value}</p><p className="mt-1 text-xs text-[var(--muted)]">{detail}</p></div>}
