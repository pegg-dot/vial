import type { Metadata } from "next";
import Link from "next/link";
import { EyeOff, FlaskConical, PackageSearch, Users } from "lucide-react";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export const dynamic = "force-dynamic";
export const metadata:Metadata={title:"Independent testing",description:"Explore VIAL sampling models and active fictional testing programs."};
export default async function Page(){const d=await getEvidenceNetworkDashboard();return <div className="mx-auto max-w-[1320px] px-5 py-16 sm:px-8 sm:py-24"><p className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-700">Independent testing</p><h1 className="mt-4 max-w-5xl text-5xl font-semibold tracking-[-.065em] sm:text-7xl">The question is not only what passed. It is who selected the sample.</h1><p className="mt-6 max-w-3xl text-base leading-7 text-black/55">VIAL distinguishes vendor-selected samples, customer-submitted sealed samples, blind purchases, and repeated multi-source surveillance.</p><div className="mt-10 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[["S1",FlaskConical,"Vendor selected","The vendor controls which unit reaches the laboratory."],["S2",PackageSearch,"Customer sealed","A customer submits a sealed unit with documented custody."],["S3",EyeOff,"Blind purchase","An independent sampler purchases without seller selection."],["S4",Users,"Multi-source agreement","Separately sourced samples produce consistent observations."]].map(([code,Icon,title,text])=><div key={String(code)} className="rounded-[28px] border border-black/[.07] bg-white p-6"><span className="grid size-11 place-items-center rounded-2xl bg-cyan-50"><Icon className="size-5 text-cyan-700"/></span><p className="mt-5 text-xs font-semibold uppercase tracking-[.16em] text-cyan-700">{String(code)}</p><h2 className="mt-2 text-xl font-semibold">{String(title)}</h2><p className="mt-3 text-sm leading-6 text-black/50">{String(text)}</p></div>)}</div><section className="mt-14"><h2 className="text-3xl font-semibold tracking-[-.04em]">Active programs</h2><div className="mt-6 space-y-4">{d.programs.map((p:Record<string,unknown>)=><div key={String(p.id)} className="rounded-[30px] border border-black/[.07] bg-white p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-700">{String(p.sampling_model).replaceAll("_"," ")}</p><h3 className="mt-2 text-2xl font-semibold">{String(p.name)}</h3><p className="mt-3 max-w-3xl text-sm leading-6 text-black/50">{String(p.public_summary)}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-800">{String(p.status)}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-black/[.03] p-4"><p className="text-xs text-black/35">Samples</p><p className="mt-1 text-xl font-semibold">{String(p.collected_sample_count)}/{String(p.target_sample_count)}</p></div><div className="rounded-2xl bg-black/[.03] p-4"><p className="text-xs text-black/35">Funding</p><p className="mt-1 text-xl font-semibold">${Number(p.funding_collected).toFixed(0)}</p></div><div className="rounded-2xl bg-black/[.03] p-4"><p className="text-xs text-black/35">Sponsor</p><p className="mt-1 text-sm font-semibold capitalize">{String(p.sponsor_type)}</p></div></div></div>)}</div></section><Link href="/passports" className="mt-10 inline-flex rounded-full bg-black px-6 py-3 text-sm font-semibold text-white">View resulting passports</Link></div>}
