export function CommerceTable({rows,columns,empty="No records yet."}:{rows:Array<Record<string,unknown>>;columns:Array<{key:string;label:string}>;empty?:string}){return <div className="overflow-x-auto rounded-[24px] border border-black/[.07] bg-white p-5"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs text-[var(--muted)]"><tr>{columns.map(c=><th key={c.key} className="pb-3 pr-4">{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id??i)} className="border-t border-black/[.06]">{columns.map(c=><td key={c.key} className="py-4 pr-4">{String(r[c.key]??"—")}</td>)}</tr>)}</tbody></table>{!rows.length&&<p className="py-8 text-center text-sm text-[var(--muted)]">{empty}</p>}</div>}
