export function CommerceTable({rows,columns,empty="No records yet."}:{rows:Array<Record<string,unknown>>;columns:Array<{key:string;label:string}>;empty?:string}){return <div className="ink hard overflow-x-auto rounded-[18px] bg-white p-5"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs font-extrabold uppercase tracking-[.06em] text-[var(--muted)]"><tr>{columns.map(c=><th key={c.key} className="pb-3 pr-4">{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id??i)} className="border-t border-[#111214]/10">{columns.map(c=><td key={c.key} className="py-4 pr-4 font-medium text-[#111214]">{String(r[c.key]??"—")}</td>)}</tr>)}</tbody></table>{!rows.length&&<p className="py-8 text-center text-sm font-medium text-[var(--muted)]">{empty}</p>}</div>}
