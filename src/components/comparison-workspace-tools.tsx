"use client";
import { Check, Save } from "lucide-react";
import { useState } from "react";
import { useMarketplace } from "./marketplace-state";
export function ComparisonWorkspaceTools(){const{compare}=useMarketplace();const[name,setName]=useState("");const[saved,setSaved]=useState(false);async function save(){if(name.trim().length<2||compare.length===0)return;const response=await fetch("/api/v1/comparisons",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,listingSlugs:compare})});if(response.ok){setSaved(true);setName("");setTimeout(()=>setSaved(false),1600)}}return <div className="mt-7 flex flex-col gap-3 rounded-[18px] ink bg-white p-4 hard sm:flex-row"><input value={name} onChange={event=>setName(event.target.value)} aria-label="Comparison name" placeholder="Name this comparison" className="field flex-1"/><button type="button" onClick={save} disabled={compare.length===0} className="ink hard-sm press flex min-h-13 items-center justify-center rounded-[12px] bg-[#111214] px-5 text-sm font-bold text-white disabled:opacity-40">{saved?<><Check className="mr-2 inline size-4"/>Saved</>:<><Save className="mr-2 inline size-4"/>Save snapshot</>}</button></div>}
