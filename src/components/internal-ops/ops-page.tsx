// Admin control-plane page — hard design system, ROYAL-BLUE signature (#2b31d8). Ink borders,
// hard offset shadows, flat fills, bold type. Shared by every generic /admin/* records view.
function text(value:unknown){if(value==null)return"—";if(typeof value==="object")return JSON.stringify(value);if(typeof value==="boolean")return value?"Yes":"No";return String(value)}
export function OpsPage({eyebrow,title,description,stats,rows,columns}:{eyebrow:string;title:string;description:string;stats:Array<[string,string|number]>;rows:Array<Record<string,unknown>>;columns:Array<{key:string;label:string}>}){return <div><div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#2b31d8]">{eyebrow}</p><h1 className="mt-2 text-4xl font-extrabold tracking-[-.045em] text-[#111214]">{title}</h1><p className="mt-3 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">{description}</p></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([l,v])=><div key={l} className="ink hard rounded-[18px] bg-white p-5"><p className="text-[11px] font-bold uppercase tracking-[.1em] text-[var(--muted)]">{l}</p><p className="mt-2 text-2xl font-extrabold text-[#111214]">{v}</p></div>)}</div><section className="ink hard mt-8 overflow-hidden rounded-[20px] bg-white"><div className="border-b-2 border-[#111214] p-6"><h2 className="text-xl font-extrabold text-[#111214]">Operational records</h2><p className="mt-1 text-sm font-medium text-[var(--muted)]">Fictional sandbox data persisted in VIAL&rsquo;s internal control plane.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#111214]/[.04] text-xs font-extrabold uppercase tracking-[.06em] text-[var(--muted)]"><tr>{columns.map(c=><th key={c.key} className="px-5 py-3">{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id??i)} className="border-t border-[#111214]/10">{columns.map((c,j)=><td key={c.key} className={`max-w-[320px] px-5 py-4 align-top ${j===0?"font-bold text-[#111214]":"font-medium text-[var(--muted)]"}`}><span className="line-clamp-3">{text(r[c.key])}</span></td>)}</tr>)}</tbody></table></div></section></div>}
