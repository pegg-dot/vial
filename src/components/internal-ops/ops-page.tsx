function text(value:unknown){if(value==null)return"—";if(typeof value==="object")return JSON.stringify(value);if(typeof value==="boolean")return value?"Yes":"No";return String(value)}
export function OpsPage({eyebrow,title,description,stats,rows,columns}:{eyebrow:string;title:string;description:string;stats:Array<[string,string|number]>;rows:Array<Record<string,unknown>>;columns:Array<{key:string;label:string}>}){return <div><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-violet-600">{eyebrow}</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.05em]">{title}</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-[var(--muted)]">{description}</p></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([l,v])=><div key={l} className="rounded-[22px] border border-black/[.07] bg-white p-5"><p className="text-xs text-[var(--muted)]">{l}</p><p className="mt-2 text-2xl font-semibold">{v}</p></div>)}</div><section className="mt-8 overflow-hidden rounded-[28px] border border-black/[.07] bg-white"><div className="border-b border-black/[.06] p-6"><h2 className="text-xl font-semibold">Operational records</h2><p className="mt-1 text-sm text-[var(--muted)]">Fictional sandbox data persisted in VIAL’s internal control plane.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-black/[.025] text-xs text-[var(--muted)]"><tr>{columns.map(c=><th key={c.key} className="px-5 py-3 font-medium">{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id??i)} className="border-t border-black/[.06]">{columns.map((c,j)=><td key={c.key} className={`max-w-[320px] px-5 py-4 align-top ${j===0?"font-semibold":"text-[var(--muted)]"}`}><span className="line-clamp-3">{text(r[c.key])}</span></td>)}</tr>)}</tbody></table></div></section></div>}
