export function LabHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <header><p className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-700">{eyebrow}</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.055em] sm:text-5xl">{title}</h1><p className="mt-4 max-w-3xl text-sm leading-6 text-black/55">{description}</p></header>; }
export function LabStat({ label, value, detail }: { label: string; value: React.ReactNode; detail?: string }) { return <div className="rounded-[24px] border border-black/[.07] bg-white p-5"><p className="text-[10px] font-semibold uppercase tracking-[.14em] text-black/35">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p>{detail&&<p className="mt-2 text-xs text-black/45">{detail}</p>}</div>; }
export function StatusPill({ value }: { value: unknown }) { const label=String(value??"unknown").replaceAll("_"," "); return <span className="rounded-full bg-black/[.06] px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-black/60">{label}</span>; }
export function Empty({ children }: { children: React.ReactNode }) { return <div className="rounded-[24px] border border-dashed border-black/15 bg-white/50 p-8 text-center text-sm text-black/45">{children}</div>; }
