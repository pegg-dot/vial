// Laboratory workspace — hard design system, TEAL signature (#0e8f80/#12b3a6). Ink borders, hard
// offset shadows, flat fills, bold type. Every lab page is composed from these primitives.
export function LabHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <header><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#0e8f80]">{eyebrow}</p><h1 className="mt-2 text-4xl font-extrabold tracking-[-.045em] text-[#111214] sm:text-5xl">{title}</h1><p className="mt-4 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">{description}</p></header>; }
export function LabStat({ label, value, detail }: { label: string; value: React.ReactNode; detail?: string }) { return <div className="ink hard rounded-[18px] bg-white p-5"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[var(--muted)]">{label}</p><p className="mt-2 text-2xl font-extrabold text-[#111214]">{value}</p>{detail&&<p className="mt-2 text-xs font-medium text-[var(--muted)]">{detail}</p>}</div>; }
export function StatusPill({ value }: { value: unknown }) { const label=String(value??"unknown").replaceAll("_"," "); return <span className="ink-1 rounded-full bg-[#e6fbf4] px-3 py-1 text-[10px] font-extrabold uppercase tracking-wide text-[#0e8f80]">{label}</span>; }
export function Empty({ children }: { children: React.ReactNode }) { return <div className="ink-1 rounded-[18px] border-dashed bg-white p-8 text-center text-sm font-medium text-[var(--muted)]">{children}</div>; }
