"use client";
import { formatCurrency } from "@/lib/format";
import type { Product } from "@/lib/types";
import { ArrowUpRight, Bookmark, GitCompareArrows, Star } from "lucide-react";
import Link from "next/link";
import { useMarketplace } from "./marketplace-state";
import { EvidenceBadge } from "./evidence-badge";
import { DataOriginBadge } from "./data-origin-badge";
import { ProductVisual } from "./product-visual";
export function ProductCard({product}:{product:Product}){const{catalog,isWatched,isCompared,toggleWatchlist,toggleCompare}=useMarketplace();const vendor=catalog.vendors.find(item=>item.slug===product.vendorSlug),watched=isWatched(product.slug),compared=isCompared(product.slug);return <article className="group overflow-hidden rounded-[28px] border border-black/[.07] bg-white shadow-[0_1px_2px_rgba(0,0,0,.02)] transition duration-300 hover:-translate-y-1 hover:border-black/[.12] hover:shadow-[0_24px_70px_rgba(23,25,30,.10)]"><div className="relative"><Link href={`/products/${product.slug}`} aria-hidden="true" tabIndex={-1}><ProductVisual name={product.name} quantity={product.quantity} accent={product.accent} compact decorative/></Link><div className="absolute right-3 top-3 flex gap-2"><button onClick={()=>toggleWatchlist(product.slug)} aria-label={watched?"Remove from watchlist":"Add to watchlist"} aria-pressed={watched} className={`grid size-9 place-items-center rounded-full border backdrop-blur transition ${watched?"border-black bg-black text-white":"border-white/70 bg-white/75 text-black hover:bg-white"}`}><Bookmark className={`size-4 ${watched?"fill-current":""}`}/></button><button onClick={()=>toggleCompare(product.slug)} aria-label={compared?"Remove from comparison":"Add to comparison"} aria-pressed={compared} className={`grid size-9 place-items-center rounded-full border backdrop-blur transition ${compared?"border-[var(--accent)] bg-[var(--accent)] text-white":"border-white/70 bg-white/75 text-black hover:bg-white"}`}><GitCompareArrows className="size-4"/></button></div></div><div className="p-5"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><Link href={`/products/${product.slug}`} className="inline-flex items-center gap-1.5 text-[18px] font-semibold tracking-[-0.03em] hover:underline hover:decoration-black/20 hover:underline-offset-4">{product.name} <span className="font-medium text-black/60">{product.quantity}</span><ArrowUpRight className="size-3.5 text-black/35 opacity-0 transition group-hover:opacity-100"/></Link><Link href={`/vendors/${product.vendorSlug}`} className="mt-1 block truncate text-sm text-[var(--muted)] hover:text-black">{vendor?.name}</Link></div><div className="text-right"><p className="text-[19px] font-semibold tracking-[-0.04em]">{formatCurrency(product.price)}</p>{product.previousPrice&&product.previousPrice!==product.price?<p className="mt-0.5 text-xs text-[var(--muted)] line-through">{formatCurrency(product.previousPrice)}</p>:null}</div></div><div className="mt-4 flex flex-wrap items-center gap-2"><EvidenceBadge level={product.evidenceLevel} label={product.evidenceLabel}/>{product.origin==="live"?<DataOriginBadge origin="live"/>:product.reviewCount>0?<span className="inline-flex items-center gap-1 text-xs text-[var(--muted)]"><Star className="size-3 fill-current"/> {product.rating} <span className="text-black/55">({product.reviewCount})</span></span>:null}</div><div className="mt-5 grid grid-cols-2 gap-3 border-t border-black/[.06] pt-4 text-xs"><div><p className="text-[var(--muted)]">Availability</p><p className="mt-1 font-semibold">{product.availability}</p></div><div><p className="text-[var(--muted)]">Last checked</p><p className="mt-1 font-semibold">{product.lastChecked}</p></div></div></div></article>}
