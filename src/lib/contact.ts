// PLACEHOLDERS. Every value in this file must be replaced by the owner before launch.
//
// These strings render verbatim on /legal/contact, /legal/terms and /legal/privacy — the pages that
// promise a human will answer. `.example` is a reserved TLD that can never resolve, and the bracketed
// values read as unfinished on sight, so an unreplaced value is loud rather than a dead mailbox
// quietly swallowing a vendor's dispute, a deletion request, or a takedown notice.
//
// Replacing them is the whole job: change the value here and every page that promises a reply
// updates with it.

/** Corrections, disputes, and general contact. */
export const CONTACT_EMAIL = "contact@vialgrade.example";

/** Access, correction, and deletion requests for account data. */
export const PRIVACY_EMAIL = "privacy@vialgrade.example";

/** Copyright, trademark, and other legal notices. */
export const LEGAL_EMAIL = "legal@vialgrade.example";

/** The company or individual that operates VialGrade, as it should appear in the terms. */
export const LEGAL_ENTITY = "[operating entity — to be added]";

/** Whose law governs the terms, e.g. a US state or a country. */
export const GOVERNING_LAW = "[governing law — to be added]";

/** Where disputes are heard. */
export const VENUE = "[courts and venue — to be added]";

/** Who hosts the site and stores the database, named for the privacy notice. */
export const HOSTING_PROVIDER = "[hosting and database provider — to be added]";

/** How quickly we say we will reply. Stated publicly, so keep it a number that can be met. */
export const RESPONSE_TARGET_DAYS = 10;
