import type { Metadata } from "next";
import { AlarmClock, CheckCircle2, Gauge, ShieldCheck } from "lucide-react";
import { requireStaff } from "@/server/auth/session";
import { getQualityDashboard } from "@/server/quality/repository";
export const metadata:Metadata={title:"Quality and benchmarks"};export const dynamic="force-dynamic";
export default async function QualityPage(){await requireStaff();const {parsers,queueAge}=await getQualityDashboard();return <div><p className="text-[11px] font-extrabold uppercase tracking-[.18em] text-[#2b31d8]">Production-source readiness</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-.055em] sm:text-5xl">Quality and benchmarks</h1><p className="mt-4 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">Monitor parser reliability, claim yield, review aging, and the controls that must pass before a fictional source profile can be promoted into an approved production-source program.</p><div className="mt-8 grid gap-4 sm:grid-cols-4"><Metric icon={Gauge} value={`${Math.round(parsers.reduce((s,p)=>s+p.successRate,0)/Math.max(parsers.length,1))}%`} label="Mean fetch success"/><Metric icon={AlarmClock} value={`${Math.round(queueAge.oldestHours)}h`} label="Oldest pending"/><Metric icon={ShieldCheck} value={String(parsers.length)} label="Parser contracts"/><Metric icon={CheckCircle2} value={String(parsers.reduce((s,p)=>s+p.claims,0))} label="Claims observed"/></div><section className="ink hard mt-8 overflow-hidden rounded-[20px] bg-white"><div className="border-b-2 border-[#111214] p-6"><h2 className="text-xl font-extrabold tracking-[-.03em]">Parser contract performance</h2><p className="mt-2 text-xs font-medium text-[var(--muted)]">Current figures are generated from the controlled fictional source program.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#111214]/[.04] text-[10px] font-extrabold uppercase tracking-[.12em] text-[var(--muted)]"><tr><th className="px-6 py-4">Profile</th><th>Sources</th><th>Attempts</th><th>Success</th><th>Changed</th><th>Claims</th><th>Pending</th></tr></thead><tbody className="divide-y divide-[#111214]/10">{parsers.map(p=><tr key={p.profile}><td className="px-6 py-4 font-mono text-xs">{p.profile}</td><td>{p.sources}</td><td>{p.attempts}</td><td><span className={`ink-1 rounded-full px-2.5 py-1 text-[10px] font-extrabold ${p.successRate>=95?"bg-[#e6fbf4] text-[#0e8f80]":"bg-[#fff4e0] text-[#b26a00]"}`}>{p.successRate}%</span></td><td>{p.changedSnapshots}</td><td>{p.claims}</td><td>{p.pending}</td></tr>)}</tbody></table></div></section><section className="mt-6 grid gap-4 lg:grid-cols-[1fr_1.2fr]"><div className="ink hard rounded-[20px] bg-white p-6"><h2 className="text-xl font-extrabold tracking-[-.03em]">Queue aging</h2><div className="mt-6 grid grid-cols-2 gap-3"><Age label="Under 1 hour" value={queueAge.underHour}/><Age label="1–4 hours" value={queueAge.oneToFour}/><Age label="4–24 hours" value={queueAge.fourToTwentyFour}/><Age label="Over 24 hours" value={queueAge.overDay}/></div></div><div className="ink rounded-[20px] bg-[#111214] p-6 text-white"><p className="text-[11px] font-extrabold uppercase tracking-[.16em] text-[#8fa2ff]">Promotion gate</p><h2 className="mt-3 text-2xl font-extrabold tracking-[-.04em]">A source is not production-ready because it fetched once.</h2><div className="mt-6 grid gap-3 text-xs font-medium leading-5 text-white/60"><p>✓ Explicit domain and network policy</p><p>✓ Versioned parser contract and fixture suite</p><p>✓ Accuracy benchmark against human-labeled examples</p><p>✓ Named reviewer ownership and queue SLA</p><p>✓ Failure recovery, alerting, and rollback plan</p></div></div></section></div>}
function Metric({icon:Icon,value,label}:{icon:React.ComponentType<{className?:string}>;value:string;label:string}){return <div className="ink hard rounded-[18px] bg-white p-5"><Icon className="size-4 text-[#2b31d8]"/><p className="mt-5 text-3xl font-extrabold tracking-[-.055em]">{value}</p><p className="mt-1 text-xs font-medium text-[var(--muted)]">{label}</p></div>}
function Age({label,value}:{label:string;value:number}){return <div className="ink-1 rounded-[12px] bg-[#f7f7f4] p-4"><p className="text-2xl font-extrabold">{value}</p><p className="mt-1 text-[10px] font-medium text-[var(--muted)]">{label}</p></div>}
