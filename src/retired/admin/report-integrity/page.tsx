import { requirePermission } from "@/server/auth/session";
import { getEvidenceNetworkDashboard } from "@/server/evidence-network/repository";
export default async function Page(){await requirePermission("evidence:read");const d=await getEvidenceNetworkDashboard();return <div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[#2b31d8]">Quality oversight</p><h1 className="mt-2 text-4xl font-extrabold tracking-[-.05em]">Report integrity</h1><p className="mt-3 max-w-3xl text-sm font-medium leading-6 text-[var(--muted)]">Issued, amended, superseded, and revoked reports remain in the historical record. Open conflicts are explicit and block simplified conclusions.</p><section className="mt-8"><h2 className="text-xl font-extrabold">Reports</h2><div className="mt-4 space-y-3">{d.reports.map((r:Record<string,unknown>)=><div key={String(r.id)} className="ink hard rounded-[18px] bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-mono text-xs text-[#2b31d8]">{String(r.report_number)} · v{String(r.version)}</p><p className="mt-2 font-bold">{String(r.laboratory_name)} · Sample {String(r.sample_code)}</p></div><span className="ink-1 rounded-full bg-[#f7f7f4] px-3 py-1 text-xs font-extrabold capitalize">{String(r.status)}</span></div></div>)}</div></section><section className="mt-8"><h2 className="text-xl font-extrabold">Open conflicts</h2><div className="mt-4 space-y-3">{d.conflicts.map((c:Record<string,unknown>)=><div key={String(c.id)} className="ink-1 hard-sm rounded-[18px] bg-[#fff4e0] p-5"><div className="flex items-center justify-between"><p className="font-bold capitalize">{String(c.dimension)} · {String(c.declared_batch_code)}</p><span className="text-xs font-extrabold uppercase text-[#b26a00]">{String(c.severity)}</span></div><p className="mt-2 text-sm font-medium leading-6 text-[#b26a00]">{String(c.explanation)}</p></div>)}</div></section></div>}
