import { requireLaboratoryPermission } from "@/server/auth/session";
import { getLaboratoryContext } from "@/server/evidence-network/repository";
import { LabHeader, StatusPill } from "@/components/laboratory/laboratory-ui";
export default async function Page(){const p=await requireLaboratoryPermission("lab:onboarding:write");const c=await getLaboratoryContext(p.email);if(!c)return null;return <div><LabHeader eyebrow="Laboratory onboarding" title="Competence is scoped, not assumed." description="Identity, impartiality, quality-system, method scope, team roles, security, and agreements are reviewed independently before a laboratory can participate."/><div className="mt-8 ink-1 hard rounded-[18px] bg-white p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm font-medium text-[var(--muted)]">Completion</p><p className="mt-1 text-3xl font-extrabold">{Number(c.onboarding?.completion_percent??0)}%</p></div><StatusPill value={c.onboarding?.status}/></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-[#e6fbf4]"><div className="h-full rounded-full bg-[#12b3a6]" style={{width:`${Number(c.onboarding?.completion_percent??0)}%`}}/></div></div><div className="mt-6 grid gap-3 md:grid-cols-2">{c.steps.map((s:Record<string,unknown>)=><div key={String(s.id)} className="ink-1 hard rounded-[18px] bg-white p-5"><div className="flex items-center justify-between gap-3"><h2 className="font-extrabold capitalize">{String(s.step_key).replaceAll("_"," ")}</h2><StatusPill value={s.status}/></div><p className="mt-3 text-xs font-medium leading-5 text-[var(--muted)]">This dimension is independently reviewable and cannot be completed by a report upload alone.</p></div>)}</div></div>}
