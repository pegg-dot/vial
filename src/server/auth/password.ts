import{randomBytes,scrypt as nodeScrypt,timingSafeEqual}from"node:crypto";import{promisify}from"node:util";const scrypt=promisify(nodeScrypt),KEY_LENGTH=64,PREFIX="scrypt-v1";export async function hashPassword(password:string){if(password.length<12)throw new Error("Password must be at least 12 characters");const salt=randomBytes(16),derived=await scrypt(password,salt,KEY_LENGTH)as Buffer;return`${PREFIX}$${salt.toString("base64url")}$${derived.toString("base64url")}`}export async function verifyPassword(password:string,encoded:string){const[p,s,h]=encoded.split("$");if(p!==PREFIX||!s||!h)return false;try{const salt=Buffer.from(s,"base64url"),expected=Buffer.from(h,"base64url"),actual=await scrypt(password,salt,expected.length)as Buffer;return actual.length===expected.length&&timingSafeEqual(actual,expected)}catch{return false}}
